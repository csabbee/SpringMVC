<%@taglib tagdir="/WEB-INF/tags"  prefix="b"%>
<h2>Lunch</h2>

Minutes left : ${minutes}

<b:debug title="page" map="${pageScope}" />
<b:debug title="request" map="${requestScope}" />
<b:debug title="app" map="${applicationScope}" />